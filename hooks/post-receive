#!/usr/bin/env bash
set -euo pipefail

pushd ..
# shellcheck disable=SC2046
( unset $(git rev-parse --local-env-vars); git submodule update --init --recursive )
hugo --enableGitInfo --minify
rm -vrf /srv/http/xtex
mv -v public /srv/http/xtex
popd
