jtd:
  optionalProperties:
    descr:
      type: string
  properties:
    auth:
      elements:
        type: string
    mail:
      elements:
        type: string
    mntner:
      type: string
    source:
      type: string
ref:
  - path: source
    schema: REGISTRY
schema: MNTNER
