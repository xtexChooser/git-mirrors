jtd:
  optionalProperties:
    descr:
      type: string
  properties:
    asn:
      type: uint32
    aut_num:
      type: string
    net:
      type: string
    source:
      type: string
ref:
  - path: source
    schema: REGISTRY
  - path: asn
    schema: AUT-NUM
  - path: net
    schema: NET
schema: AUT-NUM
