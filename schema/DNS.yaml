jtd:
  optionalProperties:
    descr:
      type: string
  properties:
    dns:
      type: string
    net:
      type: string
    record:
      values:
        elements:
          type: string
    source:
      type: string
ref:
  - path: source
    schema: REGISTRY
  - path: net
    schema: NET
schema: DNS
